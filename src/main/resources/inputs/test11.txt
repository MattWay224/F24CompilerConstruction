(setq counter 0)
(while (less counter 5)
  (setq counter (plus counter 1))
  (print counter)
)

(setq sum 0)
(setq n 1)
(while (less n 6)
  (setq sum (plus sum n))
  (setq n (plus n 1)))

(print sum)
(print n)